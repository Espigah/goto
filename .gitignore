# binaries
/goto
/build/
*.AppImage
/AppDir/
*.exe         # Windows binaries are CI/release artifacts, never committed
*~            # editor backup files (e.g. goto.exe~)
*.syso        # generated in Windows CI from goto.rc (the exe icon resource)

# compiled whisper.cpp lib
/third_party/whisper.cpp/build_go/

# downloaded models (do not version)
*.bin

# goreleaser artifacts
/dist/
