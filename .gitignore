# binaries
/goto
/build/
*.AppImage
/AppDir/
goto.exe
goto_*.exe
goto_noaudio.exe
*~

# generated Windows resource (windres output)
*.syso
rsrc_windows_amd64.syso

# compiled whisper.cpp lib
/third_party/whisper.cpp/build_go/

# downloaded models (do not version)
*.bin

# goreleaser artifacts
/dist/

# Windows build helpers — compiled locally, never committed
gcc_wrap.exe
gcc_wrapper.bat
test-build.ps1

# wrapper/ folder with GCC binaries (local dev only)
wrapper/

# scratch / debug files left from build investigation
scripts/test.c
scripts/test.o
scripts/test2.o
nm1.txt
nm2.txt
wrapper.log
